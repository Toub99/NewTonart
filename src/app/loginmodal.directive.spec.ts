import { LoginmodalDirective } from './loginmodal.directive';

describe('LoginmodalDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginmodalDirective();
    expect(directive).toBeTruthy();
  });
});
